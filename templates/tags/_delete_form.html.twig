<form method="post" action="{{ path('app_tags_delete', {'id_tag': tag.id_tag}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id_tag) }}">
    <button class="btn">Delete</button>
</form>
